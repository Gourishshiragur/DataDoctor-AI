streamlit>=1.35.0
requests>=2.31.0
# RAG + vector store — no version pins so pip resolves what works on Python 3.14
chromadb>=0.5.0
sentence-transformers>=3.0.0
# Pin Pillow high enough to have Python 3.14 wheels pre-built
Pillow>=11.0.0
numpy>=1.26.0
torchvision